# Tablas estructuradas

Tablas de referencia (p. ej. **tabla SOAT — Decreto 056/2015**) usadas para
contrastar el quantum reclamado en casos de tránsito (Paso 5 — perjuicio).

Cada tabla es una fuente más del corpus: debe tener `id`, `fuente`, `texto`/datos
verificados y `url` oficial, igual que el resto (ver `corpus/README.md`).

> Pendiente de cargar por los abogados.
